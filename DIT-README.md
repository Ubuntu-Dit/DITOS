This is an os called DITOS it is an command os.
Run this in terminal/cmd if DITOS is in your homefile then write the command : ./DITOS.
This os is an Linux os it uses debain commands and a extra (/restart)
This os has no games.
All the directory is from your PC.
